use ...primitives
use ..iterable

public trait Collection<Item>: Iterable<Item> {
  fun /* let */ isEmpty(): Bool
  fun /* let */ size(): Int /* UInt */
  // TODO(JonasWanke): let indices: Range<UInt> => 0..size

  fun contains(item: Item): Bool
  // TODO(JonasWanke): containsAll?
}

public trait MutableCollection<Item>: Collection<Item> {
  fun add(item: Item)
  fun set(index: Int /* UInt */, item: Item)
  // TODO(JonasWanke): addAll?
  // fun clear()
}
