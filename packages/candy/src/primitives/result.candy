/*
trait Result<Success, Error>
/// Represents the result of an operation that might fail.

class Ok<Success> {
  /// The result of an operation that succeeded.

  let value: Success
}
impl<Error> Ok<Success>: Result<Success, Error>

class Error<ErrorType> {
  /// The result of an operation that failed.

  let error: ErrorType
}
impl<Success> Error<ErrorType>: Result<Success, ErrorType>
*/
