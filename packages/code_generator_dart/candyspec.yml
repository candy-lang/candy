name: code_generator_dart

dependencies:
  petit_parser:
    # remove this when we support transitive dependencies
    path: ../petit_parser
  incremental:
    path: ../incremental
  compiler_utils:
    path: ../compiler_utils
  cst:
    # remove this when we support transitive dependencies
    path: ../cst
  ast:
    # remove this when we support transitive dependencies
    path: ../ast
  hir:
    path: ../hir
  dart_code:
    path: ../dart_code
