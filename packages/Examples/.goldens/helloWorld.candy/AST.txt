# AST for module Examples:helloWorld
assignment: identifier $6@"builtins" = 
  call identifier $0@"use" with these arguments:
    text
      textPart $2@"Builtins"
assignment: identifier $24@"main" := 
  function (non-fuzzable) { $9@"environment" ->
    call struct access identifier $14@"builtins".$16@"ChannelSend" with these arguments:
      struct access identifier $10@"environment".$12@"Stdout"
      text
        textPart $18@"Hello, world!"
  }
