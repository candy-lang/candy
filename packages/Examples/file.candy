[print] = use "Builtins"
[file] = use "FileSystem"
[result, text] = use "Core"

main := { environment ->
  (path,) = environment.arguments

  bytes = file.readBytes environment.fileSystem.file path | result.unwrap
  print "Bytes: {bytes}"
}
