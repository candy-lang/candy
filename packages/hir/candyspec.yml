name: hir

dependencies:
  petit_parser:
    # remove this when we support transitive dependencies
    path: ../petit_parser
  compiler_utils:
    path: ../compiler_utils
  cst:
    # remove this when we support transitive dependencies
    path: ../cst
  ast:
    path: ../ast
  incremental:
    path: ../incremental
