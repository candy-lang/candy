text = use "..text"

todo message :=
  needs (text.is message)
  ## There is no way to call `todo` correctly.
  ## TODO: Use text interpolation when available.
  needs False (text.concatenate "TODO: " message)
