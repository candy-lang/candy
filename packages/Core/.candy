bool = use ".Bool"
conditionals = use ".Conditionals"
equals = builtinStructGet (use ".Equality") Equals
function = use ".Function"
int = use ".Int"
check = builtinStructGet (use ".Needs") Check
needs = builtinStructGet (use ".Needs") Needs
panic = builtinStructGet (use ".Panic") Panic
struct = use ".Struct"
text = use ".Text"
type = use ".Type"
