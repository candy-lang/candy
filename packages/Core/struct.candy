type = use "..type"

is value := type.is value Struct

hasKey struct key :=
  needs (is struct)
  struct | ✨.structHasKey key

getUnwrap struct key :=
  needs (is struct)
  needs (hasKey struct key)
  struct | ✨.structGet key

getKeys struct :=
  needs (is struct)
  struct | ✨.structGetKeys
