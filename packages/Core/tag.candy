type = use "..type"

is value := type.is value Tag

getSymbol tag :=
  needs (is tag)
  tag | ✨.tagGetSymbol

hasValue tag :=
  needs (is tag)
  tag | ✨.tagHasValue

getValue tag :=
  needs (is tag)
  needs (hasValue tag)
  tag | ✨.tagGetValue
