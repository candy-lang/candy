use ..Hash
use ..Operators
use ..Primitives
use ..String

public builtin type Int

impl Int {
  public static builtin fun parse(value: String): Int
}

builtin impl Int: Equals & Comparable
builtin impl Int: Add & Subtract & Negate & Multiply & DivideTruncating & Modulo

impl Int: Hash {
  fun hash[Result](hasher: Hasher[Result]): Hasher[Result] { hasher.write(this) }
}
