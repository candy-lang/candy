# Structs are types that can hold multiple other types.
#
# There is nothing to see here, because structs are actually built into the compiler. Just use
# parentheses to create them on-the-fly:
#
# ```
# let b: (foo: String, bar: Int) = (foo: "foo", bar: 42) # Struct
# ```
