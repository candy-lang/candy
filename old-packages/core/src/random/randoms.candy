use ..source

public trait Random {
  fun randomSample(source: RandomSource): This
}

// fun random<T: Random>(source: RandomSource): T { T.randomSample(source) }


// TODO(never, JonasWanke): add this when we have ranges
// trait RandomInRange: Comparable {
//   fun randomInRange(range: Range<This>): This
// }
