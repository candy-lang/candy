{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Compiler: Debug Fibonacci",
      "type": "lldb",
      "request": "launch",
      "preLaunchTask": "Compiler: Build (debug)",
      "program": "${workspaceRoot}/target/debug/candy",
      "args": ["run", "packages/Examples/fibonacci.candy"],
      "cwd": "${workspaceRoot}"
    }
  ]
}
